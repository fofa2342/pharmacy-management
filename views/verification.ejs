<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enregistrement du Personnel - SIH Femmes Enceintes</title>
    <link rel="stylesheet" href="css/styles.css">
    <style>
        /* Réinitialisation des styles par défaut */
        body,
        h1,
        h2,
        p,
        ul,
        li {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        body {
            display: flex;
            min-height: 100vh;
            background-color: #f4f4f4;
        }

        /* Sidebar (menu latéral) */
        .sidebar {
            width: 250px;
            background-color: #4CAF50;
            color: white;
            padding: 20px;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
        }

        .sidebar h2 {
            margin-bottom: 20px;
            font-size: 1.5em;
        }

        .sidebar nav ul {
            list-style-type: none;
        }

        .sidebar nav ul li {
            margin: 15px 0;
        }

        .sidebar nav ul li a {
            color: white;
            text-decoration: none;
            font-size: 1.1em;
        }

        .sidebar nav ul li a:hover {
            text-decoration: underline;
        }

        /* Contenu principal */
        .content {
            flex: 1;
            padding: 20px;
            background-color: white;
            margin-left: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .content header {
            margin-bottom: 20px;
        }

        .content h1 {
            color: #333;
            font-size: 2em;
        }

        .content h2 {
            color: #4CAF50;
            font-size: 1.8em;
            margin-bottom: 10px;
        }

        /* Conteneur pour aligner les formulaires horizontalement */
        .form-container {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            gap: 20px;
            flex-wrap: wrap; /* Pour s'adapter aux petits écrans */
            margin-bottom: 20px;
        }

        /* Style des formulaires */
        .form-container form {
            flex: 1;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            min-width: 280px; /* Pour éviter que les formulaires deviennent trop petits */
        }

        .form-container label {
            font-weight: bold;
            display: block;
            margin-bottom: 5px;
        }

        .form-container input,
        .form-container select {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .form-container input[type="submit"] {
            background-color: #4CAF50;
            color: white;
            cursor: pointer;
        }

        .form-container input[type="submit"]:hover {
            background-color: #45a049;
        }

        /* Styles pour le tableau des enregistrements */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        table, th, td {
            border: 1px solid #ddd;
        }

        th, td {
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #4CAF50;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        /* Pied de page */
        footer {
            text-align: center;
            padding: 10px;
            background-color: #333;
            color: white;
            position: fixed;
            bottom: 0;
            width: 100%;
        }

        /* Date et heure en haut à droite */
        #datetime {
            position: fixed;
            top: 10px;
            right: 10px;
            background-color: rgba(0, 0, 0, 0.7);
            color: white;
            padding: 8px 12px;
            border-radius: 5px;
            font-size: 1em;
            font-weight: bold;
        }

        fieldset {
            border: 1px solid #ccc;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
        }
        legend {
            font-weight: bold;
            color: #4CAF50;
        }
        .form-group {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
        }
        .form-group label, .form-group input, .form-group select {
            flex: 1;
            min-width: 150px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
            max-width: 10px; /* Définir une largeur maximale pour chaque cellule */
            word-wrap: break-word; /* Permettre au texte de revenir à la ligne */
            overflow-wrap: break-word; /* Alternative moderne à word-wrap */
        }
        th {
            background-color: #4CAF50;
            color: white;
        }
        h1,h2{
            text-align: center;
        }
    </style>
</head>

<body>
    <!-- Sidebar (menu latéral) -->
    <aside class="sidebar">
        <h2>Menu</h2>
        <nav>
            <ul>
                <li><a href="/">Accueil</a></li>
                <li><a href="/logout">Déconnexion</a></li>
            </ul>
        </nav>
    </aside>

    <!-- Contenu principal -->
    <main class="content">
        <p id="datetime"></p>

        <script>
            function updateDateTime() {
                const now = new Date();
                const date = now.toLocaleDateString('fr-FR');
                const time = now.toLocaleTimeString('fr-FR');
                document.getElementById('datetime').textContent = `${date} ${time}`;
            }

            setInterval(updateDateTime, 1000);
            updateDateTime();
        </script>
        <header>
            <h1>Vérification des produits existants</h1>
        </header>
        <fieldset>
            <legend>Filtrer les Produits</legend>
            <form action="/verification" method="GET" class="form-group">
                <label>id : <input type="number" name="id"></label>
                <label>Nom : <input type="text" name="nom"></label>
                <label>Prix : <input type="number" name="prix"></label>
                <label>Voies : <input type="text" name="voies"></label>
                <label>Forme : <input type="text" name="forme"></label>
                <label>Stock : <input type="number" name="quantite"></label>
                <input type="submit" value="Filtrer">
            </form>
        </fieldset>
    
        <section>
            <!-- Tableau pour afficher les enregistrements -->
            <h2>Liste des produits existants</h2>
            <table>
                <thead>
                    <tr>
                        <th>id</th>
                        <th>Nom</th>
                        <th>Prix</th>
                        <th>Voie(s)</th>
                        <th>Forme</th>
                        <th>Stock</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (stock && stock.length > 0) { %>
                        <% stock.forEach(product => { %>
                            <tr>
                                <td><%= product.id %></td>
                                <td><%= product.nom %></td>
                                <td><%= product.prix %></td>
                                <td><%= product.voie %></td>
                                <td><%= product.forme %></td>
                                <td><%= product.quantite %></td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="6">Aucun produit trouvé.</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>

        </section>
    </main>

    <!-- Pied de page -->
    <footer>
        <p>&copy; 2025 SIGPP</p>
    </footer>
</body>

</html>